[pytest]
DJANGO_SETTINGS_MODULE = notification_service.settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --verbose
    --tb=short
    --strict-markers
    --disable-warnings
testpaths = tests
